# Web Worker dnet example

Send messages to/from web worker.

## Usage

Navigate to example directory.

Build using `build.sh` script:
```bash
./build.sh
```

Navigate to `www` directory and start a HTTP server there (you may use [Host These Things Please](https://github.com/thecoshman/http) for that purpose):
```bash
cd www
http
```
And open the site in the browser.

## See also

[Web Workers API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API) - Web Workers MDN documentation page.
