# RPC dnet example

Example demonstrating RPC using `dnet`.

## Usage

Navigate to `dnet` crate root directory.

First, start the server:
```bash
cargo run --example rpc -- --server
```

Start the client (in another terminal window):
```bash
cargo run --example rpc
```
