# Security

TODO

write that security is out of scope of `dnet` crate, `dnet` transports are meant to be either used in local contexts, security is handled by the underlying transport or meant to be implemented by the user on top of the `dnet` transport (not recommended, unless crypto expert)